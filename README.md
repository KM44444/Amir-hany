# CS211 Web Programming – Lecture 4 (HTML I) Assignment

This repository contains basic HTML examples based on **Lecture 4 – HTML I** (CS211 Web Programming).

## 👤 Student Information
- **Name:** Amir Hany  
- **Student ID:** 224101521  

## 📁 Project Structure
- `index.html` — Main page (entry point)
- `headings.html` — Headings examples (`h1` → `h6`)
- `paragraphs.html` — Paragraph examples (`p`)
- `links.html` — Hyperlink example (`a` + `href`)
- `images.html` — Image example (`img` + `src`, `alt`, `width`)
- `styles.html` — Inline CSS examples using the `style` attribute
- `formatting.html` — Text formatting examples (`i`, `sub`, etc.)

## ▶️ How to Run
1. Download or clone the repository.
2. Open any `.html` file directly in a web browser.

## 📝 Notes
- Styling is implemented using **Inline CSS** (`style=""`) as explained in the lecture.
- No external CSS file is used, following Lecture 2 requirements.

## 📘 Course
**CS211 – Web Programming**  
**HTML I**
